# Julie-Store 需求讨论规格

> 状态：需求仍有待定项；首个本地可用版本已实现。本文件把已经确认的需求与待定项分开；技术架构仅为建议，后续接入真实外部数据源前仍需补齐最终实现方案与验收细节。

## 1. 产品目标

Julie-Store 是移动端优先的个人图书、影视目录与评论工具。它帮助用户通过中英文作品检索建立自己的资料库，记录阅读/观看进度、每次完成的评分和评论，并按个人习惯浏览和整理。

首版是个人自用的本地应用：没有账号、公开主页、社交关系、分享流或权限系统。用户的评论、评分、标签、状态和进度只保存在自己的设备中。

## 2. 已确认的范围

### 2.1 内容分类与导航

- 资料库固定分为“图书”和“影视”两个大类；用户不能自建第三类。
- 首页提供图书和影视的独立入口，资料库页也能以标签切换两个大类。
- 首版影视不拆分电影和剧集，但内部保留类型字段，未来可扩展。
- 每个分类列表至少展示封面、标题、出版/发行日期和状态。

### 2.2 作品发现与基础资料

- 新增作品只能从检索结果保存进入资料库，不提供空白手动新增。
- 支持以书名/片名、作者/导演和可用标识查询中英文作品。
- 影视检索尽量匹配中文译名、原文名和别名；结果须展示原文名、年份、国家/地区、导演和海报等消歧信息，由用户确认候选后保存。
- 找不到罕见译名时，用户可改用其他名称再次搜索；不承诺所有译名一次命中。
- 所有自动带入的作品资料都可编辑：标题、作者/导演、日期、封面、简介、类型等。
- 同一来源、同一来源 ID、同一大类的作品不能重复保存。

### 2.3 个人目录与阅读/观看记录

- 图书状态为：想读、在读、已读、又读；影视状态为：想看、在看、已看、又看。
- 记录数值进度：图书当前页数，影视当前季/集数。已读/已看代表 100% 进度。
- “又读/又看”与多次完成历史关联。每次完成记录包含完成日期、可选十分制评分和一段评论。
- 完成记录可单独新增、编辑和永久删除；作品层面显示累计次数与最近一次记录。
- 每部作品可填固定媒介选项和自由版本说明；不同版本不拆分为不同作品。
- 标签采用“固定标签 + 自定义标签”。自定义标签支持创建、重命名、删除和合并；标签删除只移除关联，不删除作品。

### 2.4 资料库增删改查

- 可查看分类列表和单部作品详情，修改所有作品资料与个人记录。
- 支持多选作品，批量修改状态，以及追加/移除指定标签；不会覆盖未操作标签。
- 批量改为已读/已看时仅改状态，不自动生成完成日期、评分或评论；完成记录在单部作品中另行添加。
- 编辑采用“部分自动保存 + 部分明确保存”的模式；具体字段划分待定。
- 以保护个人录入信息为原则：首版不提供彻底移除个人记录。对错误的外部资料，只允许清除并重新检索/匹配，不能连带删除状态、进度、评论、完成历史、版本说明或用户添加曲目。
- 不设回收站；若未来加入彻底删除能力，需另行讨论其二次确认与影响范围。

### 2.5 首页栏目

- 首页支持创建资料库栏目，可按“正在进行”“已完成”和进度等条件选择内容。
- 栏目支持添加、删除和拖动排序。
- 首页栏目与图书/影视两个固定分类不同；前者是个人视图，后者是资料库主分类。

### 2.6 主题曲与插曲

- 主题曲/插曲仅是影视详情的补充信息，不是首版主要流程，不在首页展示，也不要求每部影视查询。
- 有可用曲目信息时，详情显示“主题曲与插曲”栏目；每首仅记录歌名和歌手。
- 不保存或分发音频、歌词，也不需要外部播放/资料链接。
- 曲目可由未来确定的合规公开来源补全，且可由用户修改；曲目缺失不得影响影视的搜索、保存和编辑。

### 2.7 导入、备份与隐私

- 支持导入用户提供的旧记录文件，目标覆盖想读/想看清单及已读/已看历史中的作品、状态、可用评分、日期和评论。
- 导入时由用户选择是否联网匹配并补全作品资料；补全不得覆盖导入的个人字段。
- 文件格式尚未确定，因此首版需先定义通用、可映射的导入格式，而不承诺特定平台文件解析器。
- 数据仅保存在当前设备。后续应支持导出与安全导入，以便用户备份或迁移；具体格式与操作流程待定。
- 不向外部元数据服务发送用户评论、评分、进度、状态、标签或完整资料库。
- 不要求豆瓣密码、Cookie 或登录，不抓取登录态网页；豆瓣或其他平台数据仅能通过用户主动提供的文件导入。

## 3. 外部作品信息来源（候选，未定稿）

应用应通过可替换的“数据源适配层”查询公开目录，而非让页面绑定某一家服务。

| 内容 | 首选候选 | 补充候选 | 典型资料 |
| --- | --- | --- | --- |
| 图书 | Google Books API | Open Library、Wikidata | 标题、作者、出版日期、出版社、ISBN、页数、封面、简介、语言 |
| 影视 | TMDB API | Wikidata、必要时 OMDb | 标题/原文名、首映日期、导演/主创、演员、类型、海报、简介、季集数 |

- 中文覆盖和字段完整度因作品及来源而异；检索结果始终是可编辑的建议资料。
- 保存来源名称及来源作品 ID 用于去重与重新匹配。
- 影视曲目是独立的补充数据问题，不能假定与影视基本资料由同一服务完整提供。
- 开发前必须按当时条款确认供应商的密钥、限流、署名、地区可用性及个人项目使用条件。

## 4. 约束与推荐架构（讨论建议）

### 约束

- 移动端优先的响应式 Web/PWA；小屏幕应可单手完成搜索、保存、更新进度和查看资料库。
- 离线时可浏览和编辑已保存资料；在线检索与外部资料补全不保证可用。
- 本地数据要在刷新和重新打开后持久保存，并能进行版本化迁移。
- 不将私密 API 密钥提交到仓库或保存进个人资料库。

### 推荐架构

```
页面与交互层
  ├── 领域服务（状态、进度、完成记录、标签、导入）
  ├── 本地资料库（IndexedDB）
  └── 作品数据源适配器
        └── 可选无状态元数据代理 → 第三方公开目录
```

- 建议采用 TypeScript 前端、IndexedDB 本地资料库和 PWA 缓存；具体框架与组件库在需求定稿后选择。
- 数据源适配器把各服务字段映射为统一的图书/影视对象，隔离 API 变动。
- 如供应商要求受保护密钥，使用不保存个人数据的服务端/边缘代理；如允许浏览器端密钥，也仅用环境配置注入并限制权限。

## 5. 待讨论的关键决策

1. 首页栏目可用的筛选条件、栏目名称、内部排序、显示数量及默认首页内容。
2. 资料库采用封面卡片、信息列表或两者切换；本地搜索、筛选和排序的具体规则。
3. 哪些字段自动保存，哪些字段（尤其评分、评论、作品资料）需要明确保存。
4. 导入文件的实际样式、字段映射、冲突处理、导出格式和备份提醒。
5. 影视曲目采用的公开来源、何时补全及用户编辑入口。
6. 是否以及何时加入“彻底移除个人记录”能力。
7. 视觉风格、底部导航和无障碍细节。

## 6. 实施步骤（需求定稿后）

1. 确认第 5 节待定项，并选定实际数据源与部署方式。
2. 建立移动端 PWA、领域数据模型和本地资料库迁移机制。
3. 实现图书/影视检索、候选消歧、保存、去重与完整资料编辑。
4. 实现分类资料库、状态/进度、标签、完成记录和批量整理。
5. 实现首页栏目、导入导出与外部资料重新匹配。
6. 补充可选曲目、离线体验、无障碍与端到端验收。

## 7. 当前验收基线（需求定稿后细化）

- 用户可从中英文检索结果确认一部图书或影视并保存，在刷新后仍能找到它。
- 用户可更新状态和进度，添加多次完成记录及十分制评分/评论，并查看历史次数。
- 用户可用固定/自定义标签整理作品，并批量更新状态或追加/移除标签。
- 用户可修改所有外部作品资料，清除错误资料后重新匹配，且个人记录不丢失。
- 图书和影视均可从首页入口或资料库标签进入；列表显示已确认的核心字段。
- 导入用户提供的文件不会覆盖已有个人字段，错误导入不会破坏本地资料库。
- 个人数据不因查询外部作品信息而被发送给第三方。

## 8. 首个本地版本范围

- 形式：移动端可交互网页应用；不接入真实作品 API，使用浏览器本地存储。
- 首屏：资料库列表，使用示例图书/影视数据展示已确认的封面、标题、出版/发行日期与状态。
- 版面：采用封面卡片视图；卡片须保留标题、日期与状态，兼顾浏览封面与管理资料库。
- 视觉方向：简洁留白，以清晰的信息层级和轻量手机操作为优先。
- 交互目标：在图书/影视大类间切换、进入单项详情占位、选择多项并展示批量修改状态/标签的入口。
- 当前实现包括：首页栏目、资料库列表、搜索候选保存、详情编辑、完成记录、曲目记录、标签管理、批量状态/标签操作、JSON/CSV 导入和 JSON 导出。
- 当前实现仍使用内置候选数据模拟自动补全；真实 Google Books/TMDB 等 API 接入留待下一阶段。
- 已确认：界面采用封面卡片和简洁留白。待定：未来生产版是否额外提供信息列表或视图切换。
- 制作状态：已创建静态本地应用，入口为 `index.html`。
